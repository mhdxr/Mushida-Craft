import { AdminInquiries } from "@/components/admin/admin-inquiries";

export const metadata = {
  title: "Admin · Inquiry WA",
  robots: { index: false, follow: false },
};

export default function AdminInquiriesPage() {
  return <AdminInquiries />;
}
