# Rencana: UX Interactions & Polish

## File yang akan diubah (4 file)

### 1. `src/components/layout/footer.tsx` — contact info jadi link
Saat ini WhatsApp number, Instagram, dan email adalah dead `<span>`. Ubah:
- WhatsApp `0812-3456-7890` → `<a href={buildWhatsAppUrl("...")}>` link (buka wa.me)
- Email `hello@Mushida.id` → `<a href="mailto:hello@Mushida.id">`
- Instagram `@Mushida.id` → `<a href="https://instagram.com/Mushida.id" target="_blank">`

Import `buildWhatsAppUrl` dari `@/lib/whatsapp` dan `getWhatsAppNumber` untuk nomor.

### 2. `src/components/ui/button.tsx` — accent variant konsisten
Tambah `hover:shadow-md hover:shadow-accent/30` pada `accent` variant agar konsisten dengan `default` variant yang punya `hover:shadow-md hover:shadow-primary/30`.

### 3. `src/components/product/product-card.tsx` — stagger cap + image guard
- **Stagger cap**: `delay: Math.min(index * 0.05, 0.4)` — batasi delay maksimum 0.4s agar card terakhir di grid besar tidak menunggu terlalu lama
- **Image guard**: `product.images[0]` → `product.images[0] ?? "/placeholder.png"` agar tidak crash jika images kosong

### 4. `src/components/home/hero.tsx` — image guard minor
Tidak perlu — hero image hardcoded Unsplash URL, sudah aman. Skip.

## Tidak diubah
- `globals.css` dead code cleanup — area berbeda (cleanup), tidak masuk scope ini
- Category icons, 404 page, skeletons — area visual polish, tidak masuk scope ini
- Framer-motion import paths — masih konsisten dengan `"framer-motion"`, tidak ada masalah

## Verifikasi
- `npm run type-check` — bersih
- `npm run lint` — bersih
- `npm run build` — sukses